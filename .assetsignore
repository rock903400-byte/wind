# 不發佈為公開靜態資產的路徑（gitignore 語法，相對於 assets directory ＝ repo 根目錄）
#
# 驗證方式：部署後 curl 這些路徑應為 404，站台頁面與 assets/ 應維持 200。
# `_headers` / `_redirects` / `.assetsignore` 由 Cloudflare 自動排除，不需列出。

# 後台管理頁：noindex 且不對外發佈
member-balance.html
# 它的 68KB 腳本也一起排除 —— 唯一的載入者是上面那頁（正式站 404），
# 發佈了也沒有任何頁面會用到，純粹是死重量兼後台邏輯外流。
# 注意：只能排除這一支，不可寫成 assets/ —— 其他頁面要用 style.css /
# components.css / tokens.css / main.js / booking.js / shared.js / theme-init.js。
assets/member-balance.js

# 內部文件：含 Google Apps Script 後端原始碼（feilu-member-api.gs /
# feilu-form-v3.gs）與 API 規格書，2026-08-31 實測全部是公開可讀的 200。
docs/

# 內部工具與測試
scripts/
tests/

# 版控與 CI 設定
.github/
.gitignore
wrangler.toml

# 依賴與工具暫存：wrangler-action 會在工作區 npm 安裝 wrangler，
# 其 node_modules（含百 MB 的 workerd）絕不可隨靜態資產上傳（25 MiB 上限）。
node_modules/
.wrangler/
